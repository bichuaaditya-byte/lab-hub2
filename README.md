# Multi-user Lab Hub

Public homepage -> choose user -> choose subject -> choose experiment.

Edit `config.js` to rename users or subjects. Keep IDs matching the folder names.

Each subject has 15 experiment slots. Each experiment page has Aim, Theory, Code, Copy Code, screenshots and Result.

To add another public user, add them to `USERS` and create a matching folder under `users/`.
